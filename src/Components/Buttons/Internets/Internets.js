import React from 'react'
import styles from './Internets.module.css'

const internets = (props) => (
  <div className={styles.btnContainer}>
    <div className={styles.internets}></div>
    <p>internets</p>
  </div>
)

export default internets
